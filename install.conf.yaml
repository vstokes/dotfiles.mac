- defaults:
    link:
      relink:  true

- clean: ['~']

- link: 
    ~/.gitconfig:
    ~/.zshrc: 
    ~/.vimrc:

- create:

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  -
  #command: ./setup_homebrew.zsh
  # stdout: true 
  #  stderr: true
  
  
